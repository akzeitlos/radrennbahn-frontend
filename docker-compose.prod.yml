services:
  radrennbahn-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    container_name: radrennbahn-frontend
    ports:
      - "3000:3000"
    restart: always